[install]
# Write exact versions (no ^ ranges) to package.json when adding packages.
exact = true

# Refuse to install any package version published less than 1 week ago.
# Mitigates supply-chain attacks from freshly published malicious releases.
minimumReleaseAge = 604800
